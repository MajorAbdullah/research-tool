Your previous answer did not include a valid citation for its claims — either it had no bracketed
source number at all, or it referenced a source number that was not in the numbered list you were
given.

Try again, following the same rules as before:

- If the sources actually support an answer, restate it with a correct `[n]` citation immediately
  after every factual claim, using only the source numbers from the numbered list you were given
  (never a number outside that list).
- If, on reflection, the sources do not actually contain information that answers the question,
  say so plainly instead — do not force a citation onto a source that does not really support the
  claim, and do not answer from general knowledge.
