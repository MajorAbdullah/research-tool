## This item is a GitHub repository — additional fields

This item is a software repository. In addition to the fields above, also include inside
`kindFields`:

- `what_it_does` — one plain-language sentence describing what the repository, library, or tool
  actually does.
- `primary_use_case` — one sentence describing the single most common reason someone would reach
  for it.

Base both fields only on the repository's own README/description inside the `<untrusted_content>`
block, not on assumptions from the repository name alone. The repository's language, star count,
license, and last-commit date are supplied separately and are not your responsibility to report.
